
<div class="student-subjects">
  <div class="well">
    <% @student_subjects.each do |subject| %>
    <ul class="well" style="background-color: #ffffff;">
      <li><%= link_to "#{subject.title}", student_subject_path(subject)%></li>
    </ul>

    <% end %>
  </div>
</div>
